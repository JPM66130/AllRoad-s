# V52.9.46 — PWA WP35

Objectif : tester AllRoad’s comme application installée, sans barre d’adresse Chrome.

- Manifest PWA en mode fullscreen, orientation libre.
- Service worker local.
- Tunnel ADB `reverse tcp:8000 tcp:8000` afin que le WP35 utilise `http://localhost:8000`, contexte sûr pour la PWA sans certificat LAN.
- Ouverture automatique de Chrome sur localhost si ADB est disponible.
- Bouton temporaire `INSTALLER ALLROAD’S` sur l’accueil hors application installée.
- Aucun changement fonctionnel aux vues Itinéraire prêt / Conduite.
